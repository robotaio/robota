import type { ProviderOptions } from '@robota/core';

/**
 * Replicate 제공업체 옵션
 */
export interface ReplicateProviderOptions extends ProviderOptions {
    apiToken: string;
}

/**
 * Replicate 제공업체 클래스
 */
export class ReplicateProvider {
    constructor(options: ReplicateProviderOptions) {
        // 초기화 로직
    }

    // 구현 예정
} 