import type { ProviderOptions } from '@robota/core';

/**
 * LangChain 제공업체 옵션
 */
export interface LangChainProviderOptions extends ProviderOptions {
    // LangChain 특정 옵션
}

/**
 * LangChain 제공업체 클래스
 */
export class LangChainProvider {
    constructor(options: LangChainProviderOptions) {
        // 초기화 로직
    }

    // 구현 예정
} 